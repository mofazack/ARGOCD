stages:
  - build
  - test

build:
  stage: build
  image: docker:latest
  services:
    - docker:dind
  script:
    - docker build -t hello-world-nodejs:latest .

test:
  stage: test
  image: node:14
  script:
    - cd app
    - npm install
    - npm test
